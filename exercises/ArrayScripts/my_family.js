let famNames = ["Ralph", "Jackie", "Jeraud", "Justin"];
let familyLength = famNames.length;
let count = 1;

for (let count = 0; count < famNames.length; count++) {
    const element = famNames[count];
    console.log(element);
}